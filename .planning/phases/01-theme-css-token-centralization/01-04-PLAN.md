---
phase: 01-theme-css-token-centralization
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/features/AboutMe/index.tsx
  - app/features/Projects/index.tsx
  - app/features/Contacts/index.tsx
autonomous: true
requirements: [THEME-01, THEME-03, THEME-04]
user_setup: []

must_haves:
  truths:
    - "AboutMe, Projects, and Contacts render all theme colors via semantic tokens and consume the shared Section for their divider/title block."
    - "InfoCard/ExperienceCard (AboutMe) and ProjectContainer (Projects) stay local per D-03 — only their color classes change."
    - "The green (WhatsApp) and blue (LinkedIn) contact-brand colors in Contacts stay literal; the email purple migrates to accent tokens."
    - "No hardcoded purple/zinc/gray class remains in these files, and each section is visually identical to its Plan 01 baseline."
  artifacts:
    - "Migrated app/features/AboutMe/index.tsx (consumes Section)"
    - "Migrated app/features/Projects/index.tsx (consumes Section)"
    - "Migrated app/features/Contacts/index.tsx (consumes Section for divider)"
  key_links:
    - "All three files import Section from ~/shared/Section and resolve colors through the @theme tokens from Plan 02."
---

<objective>
Migrate the three Section-consuming sections to tokens with zero visual change and de-duplicate their repeated gradient divider / title block via the shared `Section` (D-02, THEME-03). Per D-03, the per-feature cards (InfoCard, ExperienceCard, ProjectContainer) stay in their own files — only their colors change.

Purpose: Deliver THEME-01 + THEME-03 for the content sections while proving THEME-04 against Plan 01 baselines.
Output: 3 migrated feature files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-theme-css-token-centralization/01-CONTEXT.md
@.planning/phases/01-theme-css-token-centralization/01-PATTERNS.md
@app/shared/Section.tsx
@app/features/AboutMe/index.tsx
@app/features/Projects/index.tsx
@app/features/Contacts/index.tsx
</context>

## Token mapping (authoritative — identical to Plan 03)

Replace each literal class with its NEAREST-VALUE token; preserve opacity modifiers exactly; do NOT collapse multi-line className strings or change wording/href/icon.

| Old literal | New token |
|---|---|
| `bg-gray-950`, `bg-zinc-950` (solid) | `bg-surface` |
| `bg-zinc-950/50`, `bg-zinc-950/70` | `bg-surface/50`, `bg-surface/70` |
| `bg-zinc-900/40`, `/50`, `/70`, `/80` | `bg-surface-elevated/40`, `/50`, `/70`, `/80` |
| `text-white` | `text-on-surface` |
| `text-zinc-300`, `text-zinc-400`, `text-zinc-500` | `text-muted` |
| `bg-purple-500`, `bg-purple-500/10`, `/20` | `bg-accent`, `bg-accent/10`, `/20` |
| `bg-purple-700/10` | `bg-accent/10` |
| `bg-purple-400` | `bg-accent-hover` |
| `hover:bg-purple-500` | `hover:bg-accent` |
| `hover:bg-purple-400` | `hover:bg-accent-hover` |
| `border-purple-500` (solid) | `border-accent` |
| `border-purple-500/10`, `/20`, `/30` | `border-border-subtle/10`, `/20`, `/30` |
| `border-purple-400`, `/40`, `/50` | `border-accent-hover`, `/40`, `/50` |
| `border-zinc-950` | `border-surface` |
| `via-purple-500` | `via-accent` |
| `shadow-purple-500/10`, `/20`, `/30` | `shadow-accent/10`, `/20`, `/30` |
| `text-purple-400`, `hover:text-purple-400` | `text-accent-hover`, `hover:text-accent-hover` |
| `text-purple-300` | `text-accent-hover` |

**Leave literal (NOT part of the 7-token set, per D-01/PATTERNS):** `border-green-500`, `bg-green-500/10`, `text-green-500` (WhatsApp); `border-blue-500`, `bg-blue-500/10`, `text-blue-500` (LinkedIn). The email icon's purple (`border-purple-400 bg-purple-500/10 text-purple-400`) DOES migrate → `border-accent-hover bg-accent/10 text-accent-hover`.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate AboutMe to tokens and consume Section for its title block</name>
  <files>app/features/AboutMe/index.tsx</files>
  <read_first>
    - app/features/AboutMe/index.tsx (full file: SectionTitle, InfoCard, ExperienceCard subcomponents; title block lines 154-209; course card at 296-332)
    - app/shared/Section.tsx (final API from Plan 02)
  </read_first>
  <action>
    Apply the token mapping table to every color class in AboutMe/index.tsx: the section bg (`bg-zinc-950` → `bg-surface`), the two blur blobs, InfoCard (`border-purple-500/10 bg-zinc-900/40 hover:border-purple-500/20 hover:shadow-purple-500/10` → token equivalents), ExperienceCard (border-l `border-purple-500/20`, the dot `border-zinc-950 bg-purple-400`, `text-white`, role `text-purple-300`, dates `text-zinc-500`, activities `text-zinc-400`), the SectionTitle `text-white`, the tech-group labels `text-purple-300`, the course cards (`border-purple-500/10 bg-zinc-950/50 hover:border-purple-500/30 hover:bg-zinc-900/70`, `text-white`, `text-zinc-400`).
    Replace the title block (`<div className="mb-12 sm:mb-16">` containing divider + eyebrow "Sobre Mim" + h1 "Quem sou eu" + subtitle paragraph) with the shared `Section` from `~/shared/Section`: keep the outer `<div className="mb-12 sm:mb-16">` wrapper in place and render `<Section align="left" eyebrow="Sobre Mim" title="Quem sou eu" subtitle="Desenvolvedor Full Stack ...">` inside it, passing `titleClassName`/`subtitleClassName` only if Section's defaults don't already match AboutMe's exact heading (font-extrabold, mb-6) and subtitle (max-w-3xl, sm:text-lg) classes. Keep the "Sobre Mim"/"Quem sou eu"/subtitle text byte-identical.
    Per D-03, keep SectionTitle, InfoCard, and ExperienceCard as local subcomponents — do NOT extract a shared Card. The local `SectionTitle` (the `<h2>` used inside InfoCards) is unrelated to the shared `Section` and stays.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -Eq '(purple|zinc|gray)-[0-9]' app/features/AboutMe/index.tsx && npx playwright test tests/visual.spec.ts --grep "aboutme|home"</automated>
  </verify>
  <acceptance_criteria>
    - All theme colors in AboutMe use tokens; grep for `(purple|zinc|gray)-[0-9]` returns nothing.
    - Title block rendered via shared `Section`; outer margin wrapper retained; text unchanged.
    - InfoCard/ExperienceCard/local SectionTitle remain in-file (D-03 respected).
    - `#aboutme` and `home-full` snapshots pass within tolerance.
  </acceptance_criteria>
  <done>AboutMe uses tokens + shared Section, matches baseline, typechecks/lints clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate Projects to tokens and consume Section</name>
  <files>app/features/Projects/index.tsx</files>
  <read_first>
    - app/features/Projects/index.tsx (full file: ProjectContainer subcomponent; centered title block lines 152-191)
    - app/shared/Section.tsx (final API from Plan 02)
  </read_first>
  <action>
    Apply the token mapping table to every color class: the section bg, the two blur blobs, ProjectContainer (`border-purple-500/20 bg-zinc-900/50 hover:border-purple-400/50 hover:shadow-purple-500/20`, `text-white`, description `text-zinc-400`, the "Ver código" link `border-purple-500/20 text-zinc-400 hover:border-purple-500 hover:bg-purple-500/10 hover:text-purple-400`).
    Replace the centered title block (divider + h1 "Projetos" + subtitle) with the shared `Section`: keep the outer `<div className="mb-12 text-center sm:mb-16">` wrapper and render `<Section align="center" title="Projetos" subtitle="Projetos desenvolvidos ...">`. Projects' h1 uses `font-bold mb-4` and its subtitle uses `mx-auto max-w-2xl text-sm sm:text-base` — pass these via `titleClassName`/`subtitleClassName` so the rendered heading/subtitle match today class-for-class (Section defaults to AboutMe's styling). No eyebrow. Keep text byte-identical.
    Per D-03, ProjectContainer stays local — only its colors change.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -Eq '(purple|zinc|gray)-[0-9]' app/features/Projects/index.tsx && npx playwright test tests/visual.spec.ts --grep "projetos|home"</automated>
  </verify>
  <acceptance_criteria>
    - All theme colors in Projects use tokens; grep for `(purple|zinc|gray)-[0-9]` returns nothing.
    - Centered title block rendered via shared `Section` with Projects' exact heading/subtitle styling preserved via className overrides.
    - ProjectContainer remains in-file (D-03).
    - `#projetos` and `home-full` snapshots pass within tolerance.
  </acceptance_criteria>
  <done>Projects uses tokens + shared Section, matches baseline, typechecks/lints clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate Contacts to tokens and consume Section for the divider</name>
  <files>app/features/Contacts/index.tsx</files>
  <read_first>
    - app/features/Contacts/index.tsx (full file: section bg + blobs; standalone centered divider lines 58-69; contact card with h1/subtitle inside; three contact rows with green/purple/blue icon chips)
    - app/shared/Section.tsx (final API from Plan 02 — divider-only usage with just `align`)
  </read_first>
  <action>
    Apply the token mapping table to Contacts' theme colors: section bg, the two blur blobs, the outer contact card (`border-purple-500/10 bg-zinc-900/40`), the h1 `text-white`, the intro subtitle `text-zinc-400`, and each contact row's shared shell (`border-purple-500/10 bg-zinc-950/50 hover:border-purple-500/30 hover:bg-zinc-900/70 hover:shadow-purple-500/10`, and the `group-hover:text-zinc-300`/`text-zinc-400` value text, `text-white` labels).
    Replace ONLY the standalone gradient divider (`<div className="mx-auto mb-6 h-px w-24 bg-gradient-to-r from-transparent via-purple-500 to-transparent" />`) with `<Section align="center" />` from `~/shared/Section` (divider-only usage — Contacts' h1/subtitle stay inside the card and are NOT passed to Section, preserving structure). The card remains the divider's sibling exactly as today.
    CONTACT ICON CHIPS — apply the D-01/PATTERNS exception:
    - WhatsApp: keep `border-green-500 bg-green-500/10` and `text-green-500` LITERAL (contact-brand color, not a theme token).
    - LinkedIn: keep `border-blue-500 bg-blue-500/10` and `text-blue-500` LITERAL.
    - Email: MIGRATE `border-purple-400 bg-purple-500/10` → `border-accent-hover bg-accent/10` and icon `text-purple-400` → `text-accent-hover`.
    Keep all contact hrefs, labels, and CONTACTS references unchanged.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -Eq '(purple|zinc|gray)-[0-9]' app/features/Contacts/index.tsx && grep -Eq '(green|blue)-500' app/features/Contacts/index.tsx && npx playwright test tests/visual.spec.ts --grep "contacts|home"</automated>
  </verify>
  <acceptance_criteria>
    - No `(purple|zinc|gray)-[0-9]` literal remains in Contacts (email purple migrated to accent tokens).
    - Green (WhatsApp) and blue (LinkedIn) brand colors remain literal (grep confirms `green-500`/`blue-500` still present).
    - Divider rendered via `<Section align="center" />`; card structure and all text/hrefs unchanged.
    - `#contacts` and `home-full` snapshots pass within tolerance.
  </acceptance_criteria>
  <done>Contacts uses tokens (green/blue preserved) + shared Section divider, matches baseline, typechecks/lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Presentational className/token refactor of content sections; no input, network, auth, or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | (n/a) | AboutMe/Projects/Contacts className migration | low | accept | Presentational-only change; no runtime input surface. Zero-regression enforced by grep + Playwright diff against Plan 01 baselines. |
</threat_model>

<verification>
- `npm run typecheck`, `npm run lint`, `npm run build` pass.
- `grep -rE '(purple|zinc|gray)-[0-9]' app/features/AboutMe app/features/Projects app/features/Contacts --include='*.tsx'` returns zero matches.
- `green-500`/`blue-500` still present in Contacts (brand colors preserved).
- `npx playwright test tests/visual.spec.ts` passes for `#aboutme`, `#projetos`, `#contacts`, `home-full` within tolerance.
- All three files import `~/shared/Section`.
</verification>

<success_criteria>
- THEME-01: AboutMe/Projects/Contacts use semantic tokens (theme colors); brand colors intentionally preserved.
- THEME-03: divider/title-block de-duplicated via shared Section (D-02); cards kept local (D-03).
- THEME-04: all three sections match Plan 01 baselines within tolerance.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- Migrated `app/features/AboutMe/index.tsx` (tokens + shared Section; cards kept local).
- Migrated `app/features/Projects/index.tsx` (tokens + shared Section; ProjectContainer kept local).
- Migrated `app/features/Contacts/index.tsx` (tokens + shared Section divider; green/blue brand colors preserved).
</artifacts>

<output>
Create `.planning/phases/01-theme-css-token-centralization/01-04-SUMMARY.md` when done.
</output>

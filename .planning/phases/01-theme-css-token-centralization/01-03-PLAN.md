---
phase: 01-theme-css-token-centralization
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/features/Header/index.tsx
  - app/features/Header/types.tsx
  - app/features/Hero/index.tsx
  - app/features/Hero/types.tsx
  - app/features/Footer/index.tsx
  - app/shared/TechBadge.tsx
autonomous: true
requirements: [THEME-01, THEME-03, THEME-04]
user_setup: []

must_haves:
  truths:
    - "Header and Hero render all colors via semantic tokens and consume the shared Button; their local ActionButton/Button implementations are removed."
    - "Footer and TechBadge render all colors via semantic tokens."
    - "No hardcoded purple/zinc/gray utility class remains in any of these files, and each section is visually identical to its Plan 01 baseline."
  artifacts:
    - "Migrated app/features/Header/index.tsx (+ trimmed types.tsx)"
    - "Migrated app/features/Hero/index.tsx (+ trimmed types.tsx)"
    - "Migrated app/features/Footer/index.tsx"
    - "Migrated app/shared/TechBadge.tsx"
  key_links:
    - "Header and Hero import Button from ~/shared/Button; all four files resolve colors through the @theme tokens from Plan 02."
---

<objective>
Migrate the Button-consuming and leaf files to the token system with zero visual change: Header and Hero swap hardcoded colors for tokens AND replace their local button components with the shared `Button` (D-04, THEME-03); Footer and TechBadge get token swaps only.

Purpose: Deliver THEME-01 (token adoption) + THEME-03 (button de-dup) for the top-of-page surfaces while proving THEME-04 (zero regression) against the Plan 01 baselines.
Output: 4 migrated component files (+ 2 trimmed types files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-theme-css-token-centralization/01-CONTEXT.md
@.planning/phases/01-theme-css-token-centralization/01-PATTERNS.md
@.planning/research/PITFALLS.md
@app/shared/Button.tsx
@app/shared/TechBadge.tsx
@app/features/Header/index.tsx
@app/features/Header/types.tsx
@app/features/Hero/index.tsx
@app/features/Hero/types.tsx
@app/features/Footer/index.tsx
</context>

## Token mapping (authoritative — apply consistently in every task)

Replace each literal class with its NEAREST-VALUE token. Preserve opacity modifiers (`/10`, `/20`, `/30`, `/40`, `/50`, `/70`, `/80`) exactly. Do NOT collapse multi-line className strings or change any wording/href/icon.

| Old literal | New token |
|---|---|
| `bg-gray-950`, `bg-zinc-950` (solid) | `bg-surface` |
| `bg-zinc-950/50`, `bg-zinc-950/70` | `bg-surface/50`, `bg-surface/70` |
| `bg-zinc-900/40`, `/50`, `/70`, `/80` | `bg-surface-elevated/40`, `/50`, `/70`, `/80` |
| `text-white` | `text-on-surface` |
| `text-zinc-300`, `text-zinc-400`, `text-zinc-500` | `text-muted` (sub-perceptual collapse — intended) |
| `bg-purple-500`, `bg-purple-500/10`, `/20` | `bg-accent`, `bg-accent/10`, `/20` |
| `bg-purple-700/10` | `bg-accent/10` (decorative blur; imperceptible) |
| `bg-purple-400` | `bg-accent-hover` |
| `hover:bg-purple-500` | `hover:bg-accent` |
| `hover:bg-purple-400` | `hover:bg-accent-hover` |
| `border-purple-500` (solid) | `border-accent` |
| `border-purple-500/10`, `/20`, `/30` | `border-border-subtle/10`, `/20`, `/30` |
| `border-purple-400`, `/40`, `/50` | `border-accent-hover`, `/40`, `/50` |
| `border-zinc-950` | `border-surface` |
| `via-purple-500` | `via-accent` |
| `shadow-purple-500/10`, `/20`, `/30` | `shadow-accent/10`, `/20`, `/30` |
| `text-purple-400`, `hover:text-purple-400` | `text-accent-hover`, `hover:text-accent-hover` |
| `text-purple-300` | `text-accent-hover` (sub-perceptual collapse — intended) |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate Header to tokens and consume shared Button</name>
  <files>app/features/Header/index.tsx, app/features/Header/types.tsx</files>
  <read_first>
    - app/features/Header/index.tsx (full file: HeaderButton nav links, ActionButton lines 37-67, header/nav/mobile-menu markup)
    - app/features/Header/types.tsx (HeaderButtonProps, ActionButtonProps)
    - app/shared/Button.tsx (the shared component's final API from Plan 02)
  </read_first>
  <action>
    In `app/features/Header/index.tsx`: apply the token mapping table to every color class across HeaderButton, the logo link, the desktop Contato link, the mobile menu button, the mobile menu panel, and its links. Keep the `isMenuOpen` ternary exactly as-is (it already toggles complete literal class strings — safe per Pitfall 6).
    Remove the local `ActionButton` function entirely and replace its two usages (desktop CV button and mobile CV button) with the shared `Button` imported from `~/shared/Button`. Pass `variant="primary"`, `external`, `icon={<Download size={18} />}`, `href={CONTACTS.cvDownloadLink}`, and the exact geometry via `className` so the rendered classes match today's ActionButton class-for-class (`rounded-xl px-4 py-2.5 text-sm font-semibold` — no border, not full-width). Keep the "Baixar CV" text and the Download icon unchanged.
    In `app/features/Header/types.tsx`: remove `ActionButtonProps` (now owned by Button.tsx). Keep `HeaderButtonProps` (still used by the local HeaderButton nav component).
    Do not restructure markup, change nav labels, hrefs, or icon sizes.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -Eq '(purple|zinc|gray)-[0-9]' app/features/Header/index.tsx app/features/Header/types.tsx && npx playwright test tests/visual.spec.ts --grep "header|home"</automated>
  </verify>
  <acceptance_criteria>
    - All color classes in Header/index.tsx use tokens; grep for `(purple|zinc|gray)-[0-9]` returns nothing in Header files.
    - Local `ActionButton` removed; both CV buttons render via shared `Button` with identical classes/text/icon.
    - `ActionButtonProps` removed from types.tsx; `HeaderButtonProps` retained.
    - The `header` and `home-full` Playwright snapshots pass within tolerance (zero perceptible change).
  </acceptance_criteria>
  <done>Header uses tokens + shared Button, is visually identical to baseline, and typechecks/lints clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate Hero to tokens and consume shared Button</name>
  <files>app/features/Hero/index.tsx, app/features/Hero/types.tsx</files>
  <read_first>
    - app/features/Hero/index.tsx (full file: local Button lines 8-42 with primary/secondary ternary, blur blobs, badge, headings, tech badges, CTA buttons)
    - app/features/Hero/types.tsx (ButtonProps)
    - app/shared/Button.tsx (final API from Plan 02)
  </read_first>
  <action>
    In `app/features/Hero/index.tsx`: apply the token mapping table to every color class — the two blur blobs (`bg-purple-500/10`, `bg-purple-700/10` → `bg-accent/10`), the profile glow/border/shadow, the availability badge (`border-purple-500/30 bg-purple-500/10 text-purple-300` → `border-border-subtle/30 bg-accent/10 text-accent-hover`), the "Olá, eu sou" accent text, the h1/h2 text, and the body paragraphs.
    Remove the local `Button` function entirely and render the two CTA buttons via the shared `Button` from `~/shared/Button`: the first as `variant="primary"` and the second (`href="#contacts"`) as `variant="secondary"`. Pass the exact Hero geometry via `className` so rendered classes match today class-for-class (`rounded-lg px-5 py-3 font-bold w-full sm:w-auto`, and the primary keeps its `border border-accent`). Keep children ("Baixar CV" + Download icon; "Entre em contato" + Send icon) unchanged.
    In `app/features/Hero/types.tsx`: remove `ButtonProps` (now owned by Button.tsx). If the file becomes empty, delete it and remove its import; otherwise leave remaining types.
    Do not change wording, hrefs, image, or icon sizes.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -Eq '(purple|zinc|gray)-[0-9]' app/features/Hero/index.tsx && npx playwright test tests/visual.spec.ts --grep "inicio|home"</automated>
  </verify>
  <acceptance_criteria>
    - All color classes in Hero/index.tsx use tokens; grep for `(purple|zinc|gray)-[0-9]` returns nothing.
    - Local `Button` removed; both CTAs render via shared `Button` (primary + secondary) with identical classes/text/icons.
    - `ButtonProps` removed from Hero/types.tsx (file deleted if empty, with import cleaned up).
    - The `#inicio` and `home-full` snapshots pass within tolerance.
  </acceptance_criteria>
  <done>Hero uses tokens + shared Button, matches baseline, and typechecks/lints clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate Footer and TechBadge to tokens</name>
  <files>app/features/Footer/index.tsx, app/shared/TechBadge.tsx</files>
  <read_first>
    - app/features/Footer/index.tsx (border-purple-500/10 borders, bg-zinc-950, text-zinc-500 copyright)
    - app/shared/TechBadge.tsx (border-purple-500/20, bg-purple-500/10, hover:border-purple-400/40, hover:bg-purple-500/20, text-purple-300 icon, text-zinc-300 label)
  </read_first>
  <action>
    Apply the token mapping table to both files (leaf token swaps only — no structural change, no shared-component extraction per D-03).
    Footer: `border-purple-500/10` → `border-border-subtle/10` (both borders), `bg-zinc-950` → `bg-surface`, `text-zinc-500` → `text-muted`. Keep the copyright text and `new Date().getFullYear()` logic unchanged.
    TechBadge: `border-purple-500/20` → `border-border-subtle/20`, `bg-purple-500/10` → `bg-accent/10`, `hover:border-purple-400/40` → `hover:border-accent-hover/40`, `hover:bg-purple-500/20` → `hover:bg-accent/20`, icon `text-purple-300` → `text-accent-hover`, label `text-zinc-300` → `text-muted`. Keep the Icon/name props and layout unchanged.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -Eq '(purple|zinc|gray)-[0-9]' app/features/Footer/index.tsx app/shared/TechBadge.tsx && npx playwright test tests/visual.spec.ts --grep "footer|home"</automated>
  </verify>
  <acceptance_criteria>
    - Footer and TechBadge use only token classes; grep for `(purple|zinc|gray)-[0-9]` returns nothing in both files.
    - TechBadge props/layout and Footer copyright content unchanged.
    - The `footer` and `home-full` snapshots pass within tolerance (TechBadge appears across sections — full-page diff covers it).
  </acceptance_criteria>
  <done>Footer and TechBadge render via tokens, visually identical to baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure className/token refactor of presentational components; no input, network, auth, or data surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | (n/a) | Header/Hero/Footer/TechBadge className migration | low | accept | Presentational-only change. Pitfall 6 (dynamic class names) avoided by using the shared Button's literal variant strings; verified by grep + Playwright diff. |
</threat_model>

<verification>
- `npm run typecheck`, `npm run lint`, `npm run build` pass.
- `grep -rE '(purple|zinc|gray)-[0-9]' app/features/Header app/features/Hero app/features/Footer app/shared/TechBadge.tsx --include='*.tsx'` returns zero matches.
- `npx playwright test tests/visual.spec.ts` passes for `header`, `#inicio`, `footer`, and `home-full` within tolerance.
- Header and Hero no longer define local button components; both import `~/shared/Button`.
</verification>

<success_criteria>
- THEME-01: Header/Hero/Footer/TechBadge use semantic tokens (no literals).
- THEME-03: Header/Hero button de-duplication complete via shared Button (D-04).
- THEME-04: all affected sections match Plan 01 baselines within tolerance.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- Migrated `app/features/Header/index.tsx` (tokens + shared Button; local ActionButton removed).
- Trimmed `app/features/Header/types.tsx` (ActionButtonProps removed).
- Migrated `app/features/Hero/index.tsx` (tokens + shared Button; local Button removed).
- Trimmed/removed `app/features/Hero/types.tsx` (ButtonProps removed).
- Migrated `app/features/Footer/index.tsx` (tokens).
- Migrated `app/shared/TechBadge.tsx` (tokens).
</artifacts>

<output>
Create `.planning/phases/01-theme-css-token-centralization/01-03-SUMMARY.md` when done.
</output>

---
phase: 01-theme-css-token-centralization
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [THEME-01, THEME-02, THEME-04]
user_setup: []

must_haves:
  truths:
    - "The full migrated site passes the Playwright visual suite against the pre-refactor baselines within tolerance — zero perceptible regression across every section (THEME-04)."
    - "No hardcoded purple/zinc/gray utility class remains anywhere under app/**/*.tsx (grep-clean gate — THEME-01 definition of done)."
    - "The @custom-variant dark mechanism is present and Tailwind compiles dark: variants (THEME-02 wired)."
    - "A human has visually confirmed the before/after site is unchanged (D-07 manual sign-off, given zero automated safety net historically)."
  artifacts:
    - "Passing full visual-regression run (no new files)"
  key_links:
    - "This gate consumes the baselines from Plan 01 and the migrated files from Plans 03/04; it is the phase's authoritative zero-regression proof."
---

<objective>
Close the phase with the authoritative zero-regression proof: run the full Playwright suite against Plan 01 baselines, assert the codebase is grep-clean of literal theme colors, confirm the dark-variant mechanism compiles, and get a human before/after sign-off (D-07). This gate can only run after Plans 03 and 04 have migrated every feature file.

Purpose: Prove THEME-01 (complete token adoption), THEME-02 (mechanism wired), and THEME-04 (no visual regression) at the phase level.
Output: A green full-suite run + grep-clean assertion + human confirmation. No source files change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-theme-css-token-centralization/01-CONTEXT.md
@.planning/phases/01-theme-css-token-centralization/01-PATTERNS.md
@tests/visual.spec.ts
@app/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-clean gate + dark-variant compile check + full visual diff</name>
  <files></files>
  <read_first>
    - app/app.css (confirm the @custom-variant dark directive and 7 tokens are present)
    - .planning/phases/01-theme-css-token-centralization/01-PATTERNS.md (line 213: grep for old literal prefixes = the phase definition of done)
  </read_first>
  <action>
    Run the three automated phase gates:
    1. GREP-CLEAN (THEME-01 DoD): assert no literal theme-color utility remains in any TSX file. Scope to `*.tsx` so the legitimate `var(--color-*)` references in `app/app.css` are excluded. The green/blue contact-brand colors are intentionally retained and are NOT matched by this pattern.
    2. DARK-VARIANT (THEME-02): assert the `@custom-variant dark` directive is present in `app/app.css` and that a production build succeeds (Tailwind compiles the token utilities + dark variant).
    3. FULL VISUAL DIFF (THEME-04): run the entire Playwright suite; every section + full-page snapshot must pass against the Plan 01 baselines within the calibrated tolerance. If any snapshot fails, the diff is a real regression (missed class, layout shift, or wrong hue exceeding the sub-perceptual budget) — do NOT update baselines; fix the offending migration instead.
    Do not modify any source or baseline file in this task; it is a verification gate only.
  </action>
  <verify>
    <automated>! grep -rEq '(purple|zinc|gray)-[0-9]' app --include='*.tsx' && grep -q '@custom-variant dark' app/app.css && npm run build && npm run typecheck && npm run lint && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE '(purple|zinc|gray)-[0-9]' app --include='*.tsx'` returns zero matches.
    - `@custom-variant dark` present in app/app.css; `npm run build` succeeds.
    - Full `npx playwright test` passes against Plan 01 baselines within tolerance.
    - `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>
  <done>All automated phase gates are green: grep-clean, dark-variant compiling, and full visual suite passing with no regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human before/after visual sign-off</name>
  <what-built>The full token/component migration is complete and the automated visual suite passes. This checkpoint is the manual before/after confirmation the phase explicitly requires (D-07 / ROADMAP SC3), since the project historically had no visual-regression net.</what-built>
  <files></files>
  <action>Pause for a human to run the site and confirm the before/after appearance is visually identical (D-07 sign-off). Do not mark the phase complete until approval is given.</action>
  <read_first>
    - .planning/phases/01-theme-css-token-centralization/01-CONTEXT.md (D-07: refactor is only "done" when before/after shows no unintended visual change)
  </read_first>
  <how-to-verify>
    1. Run `npm run dev` and open the site.
    2. Scroll through every section: Header (desktop + mobile menu), Hero (badge, headings, both CTA buttons, tech badges), AboutMe (info cards, experience timeline, tech groups, courses), Projects (cards + hover), Contacts (WhatsApp green, Email purple, LinkedIn blue chips), Footer.
    3. Confirm colors, spacing, hover states, and the purple accent identity all look exactly as before the refactor. Pay attention to the intentionally-normalized greys (muted text) and the WhatsApp-green / LinkedIn-blue chips (must remain their brand colors).
    4. Optionally review the Playwright HTML report (`npx playwright show-report`) for the diff images.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the site is visually unchanged from before the refactor (accent purple intact, brand colors intact, no layout shift).
  </acceptance_criteria>
  <resume-signal>Type "approved" if visually identical, or describe any differences to fix.</resume-signal>
  <verify>
    <human-check>Human scrolls the running site and confirms zero visual change per the steps above.</human-check>
  </verify>
  <done>Human has signed off that the before/after appearance is identical (D-07 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This is a verification-only plan (no source changes).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | (n/a) | verification gates | low | accept | Read-only verification; no runtime surface introduced. |
</threat_model>

<verification>
- Grep-clean: zero `(purple|zinc|gray)-[0-9]` matches in `app/**/*.tsx`.
- `@custom-variant dark` present; `npm run build` succeeds.
- Full Playwright suite passes against Plan 01 baselines within tolerance.
- Human before/after sign-off recorded.
</verification>

<success_criteria>
- THEME-01: complete token adoption confirmed (grep-clean).
- THEME-02: dark-variant mechanism compiles.
- THEME-04: zero visual regression confirmed automatically AND by human sign-off.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- No new files — an authoritative passing verification run (grep-clean + full visual diff + build) and a recorded human sign-off.
</artifacts>

<output>
Create `.planning/phases/01-theme-css-token-centralization/01-05-SUMMARY.md` when done.
</output>

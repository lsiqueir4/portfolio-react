---
phase: 01-theme-css-token-centralization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - tests/visual.spec.ts
  - tests/visual.spec.ts-snapshots/
  - .gitignore
autonomous: false
requirements: [THEME-04]
user_setup: []

must_haves:
  truths:
    - "A Playwright visual-regression harness exists and captures per-section + full-page screenshots of the CURRENT (pre-refactor) UI as committed baselines."
    - "The harness is configured generically enough to support future behavioral tests (Phase 2 dark toggle, Phase 3 language toggle), not only screenshot diffing."
  artifacts:
    - "playwright.config.ts"
    - "tests/visual.spec.ts"
    - "tests/visual.spec.ts-snapshots/ (committed baseline PNGs)"
    - "@playwright/test devDependency in package.json"
  key_links:
    - "The baseline snapshots committed here are the diff reference every migration plan (03, 04) and the final gate (05) compare against."
---

<objective>
Establish the visual-regression safety net BEFORE any refactor touches source, and capture baseline screenshots of the current UI. Because the codebase has zero test coverage and no automated visual net (per CONCERNS.md), this harness is the only thing that can prove THEME-04 (zero visual regression) once tokens/components change.

Purpose: Give every downstream migration plan an objective, automated "did the pixels change?" check, and stand up permanent test infrastructure (D-05/D-06) reusable by Phases 2 and 3.
Output: `@playwright/test` devDependency, `playwright.config.ts`, `tests/visual.spec.ts`, and committed baseline snapshots of the pre-refactor UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-theme-css-token-centralization/01-CONTEXT.md
@.planning/phases/01-theme-css-token-centralization/01-PATTERNS.md
@package.json
@app/root.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @playwright/test package legitimacy before install</name>
  <what-built>Nothing yet — this is a supply-chain gate that must clear before any package install runs.</what-built>
  <files></files>
  <action>Pause before any package install and have a human verify that `@playwright/test` is the legitimate official package (threat T-01-SC, [ASSUMED] fallback since no RESEARCH.md audit table exists). Do not run Task 2's install until approval is given.</action>
  <read_first>
    - .planning/phases/01-theme-css-token-centralization/01-CONTEXT.md (D-05/D-06/D-07 approve Playwright as a permanent devDependency)
  </read_first>
  <how-to-verify>
    No phase RESEARCH.md Package Legitimacy Audit table exists for this run, so `@playwright/test` is treated as [ASSUMED] and must be human-verified before install (threat T-01-SC).
    1. Open https://www.npmjs.com/package/@playwright/test
    2. Confirm it is the official Microsoft-published Playwright test runner (publisher `microsoft`, millions of weekly downloads, repository github.com/microsoft/playwright).
    3. Confirm the version to be installed is the current stable release.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms @playwright/test is the legitimate official package before install proceeds.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or describe concerns.</resume-signal>
  <verify>
    <automated>MISSING — human legitimacy gate; no automated check. Install task (Task 2) must not run until approved.</automated>
  </verify>
  <done>Human has verified @playwright/test on npmjs.com and approved the install.</done>
</task>

<task type="auto">
  <name>Task 2: Install Playwright and configure the generic test harness</name>
  <files>package.json, package-lock.json, playwright.config.ts, .gitignore</files>
  <read_first>
    - package.json (existing scripts, devDependencies, no test infra today)
    - app/root.tsx (SSR app; `npm run dev` serves it — used as the Playwright webServer)
    - .planning/phases/01-theme-css-token-centralization/01-CONTEXT.md (D-06: configure generically for future behavioral tests too)
  </read_first>
  <action>
    Install the runner as a devDependency: `npm install -D @playwright/test`, then install the Chromium browser binary: `npx playwright install chromium` (chromium only — sufficient for deterministic visual baselines, avoids over-downloading).
    Create `playwright.config.ts` at the repo root configured generically (per D-06, so Phase 2/3 behavioral specs drop in without reconfiguration):
    - `testDir: "tests"`.
    - A single `chromium` project using `devices["Desktop Chrome"]` with a FIXED `viewport` (e.g. 1280x720) so screenshots are deterministic across machines.
    - `webServer`: command `npm run dev`, `url` pointing at the React Router dev server (default http://localhost:5173 — confirm the port `npm run dev` prints and use it), `reuseExistingServer: !process.env.CI`, generous `timeout`.
    - `use.baseURL` set to that same dev URL.
    - `expect.toHaveScreenshot`: set `animations: "disabled"`, and a CALIBRATED tolerance that absorbs the known sub-perceptual shade normalizations introduced by the locked 7-token palette (see 01-02-PLAN token-mapping decision) while still catching real regressions (missed classes → unstyled elements, layout shifts, wrong hue). Use `maxDiffPixelRatio: 0.02` together with a per-pixel `threshold: 0.2`. Document inline (concise comment) that this tolerance is deliberately tuned to the bounded shade-collapse decision, NOT a blanket "ignore diffs".
    Update `.gitignore` to ignore Playwright runtime output (`/test-results/`, `/playwright-report/`, `/blob-report/`, `/playwright/.cache/`) but DO commit the `tests/**-snapshots/` baselines. Also add an npm script `"test:visual": "playwright test"` to package.json for convenience.
    Do NOT add ESLint coverage changes or touch any app/ source file in this plan.
  </action>
  <verify>
    <automated>npx playwright --version && test -f playwright.config.ts && node -e "require('fs').accessSync('node_modules/@playwright/test')"</automated>
  </verify>
  <acceptance_criteria>
    - `@playwright/test` is in package.json devDependencies and installed.
    - `playwright.config.ts` exists with a chromium project, fixed viewport, `npm run dev` webServer, and the calibrated `toHaveScreenshot` tolerance.
    - `.gitignore` ignores Playwright output dirs but not the snapshot baselines.
    - No file under `app/` was modified.
  </acceptance_criteria>
  <done>Playwright is installed, the config is generic and deterministic, and runtime output is git-ignored while baselines are tracked.</done>
</task>

<task type="auto">
  <name>Task 3: Author the visual spec and capture pre-refactor baselines</name>
  <files>tests/visual.spec.ts, tests/visual.spec.ts-snapshots/</files>
  <read_first>
    - app/routes/home.tsx and app/root.tsx (section composition; Header rendered in root, sections in home route)
    - app/features/Hero/index.tsx (section id `inicio`), app/features/Projects/index.tsx (`projetos`), app/features/AboutMe/index.tsx (`aboutme`), app/features/Contacts/index.tsx (`contacts`) — the anchor ids to screenshot per-section
    - app/features/Header/index.tsx and app/features/Footer/index.tsx (header/footer landmarks)
  </read_first>
  <action>
    Create `tests/visual.spec.ts` that navigates to `/` and captures deterministic screenshots that later plans diff against:
    - A full-page screenshot (`fullPage: true`) named `home-full`.
    - Per-section screenshots by locating each anchor container: `#inicio` (Hero), `#projetos` (Projects), `#aboutme` (AboutMe), `#contacts` (Contacts).
    - The `header` landmark and the `footer` landmark.
    Before each screenshot, wait for the section to be visible and for fonts/network to settle (`waitForLoadState("networkidle")` and/or `document.fonts.ready`) so baselines are stable. Use `toHaveScreenshot()` with per-locator names.
    Run `npx playwright test` ONCE to generate the baseline snapshots (first run creates them under `tests/visual.spec.ts-snapshots/`). Commit those PNGs — they are the pre-refactor reference. Re-run once more to confirm the suite now PASSES against its own freshly-captured baselines (proves determinism / no flakiness before any refactor begins).
    Since the app currently renders in a single dark theme, these baselines represent the exact current appearance that THEME-04 requires preserving.
  </action>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/visual.spec.ts` captures full-page, per-section (Hero/Projects/AboutMe/Contacts), header, and footer screenshots.
    - Baseline PNGs exist under `tests/visual.spec.ts-snapshots/` and are committed.
    - A second consecutive `npx playwright test` run passes (deterministic, non-flaky) against the just-captured baselines.
  </acceptance_criteria>
  <done>The visual suite runs green against committed pre-refactor baselines, giving downstream plans an objective zero-regression reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | Installing `@playwright/test` + Chromium binary pulls third-party build-time code. |

This phase adds NO runtime auth, data, or network surface — it only adds a build/test-time devDependency and pure test files.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install -D @playwright/test` + `npx playwright install chromium` | high | mitigate | Blocking human-verify checkpoint (Task 1) confirms the package is the official Microsoft Playwright runner on npmjs.com before install. `[ASSUMED]` fallback applied because no phase RESEARCH.md audit table exists. |
</threat_model>

<verification>
- `npx playwright test` passes against committed baselines on two consecutive runs (deterministic).
- `npx playwright --version` resolves; `playwright.config.ts` present with fixed viewport + calibrated tolerance.
- No `app/` source file modified in this plan.
</verification>

<success_criteria>
- Permanent Playwright harness (D-05/D-06) installed and configured generically.
- Pre-refactor baseline snapshots captured and committed (D-07).
- Downstream plans have an automated zero-regression reference (THEME-04).
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `playwright.config.ts` — chromium project, fixed 1280x720 viewport, `npm run dev` webServer, `toHaveScreenshot` tolerance (`maxDiffPixelRatio: 0.02`, `threshold: 0.2`).
- `tests/visual.spec.ts` — full-page + per-section (Hero/Projects/AboutMe/Contacts) + header + footer screenshot spec.
- `tests/visual.spec.ts-snapshots/*.png` — committed pre-refactor baselines.
- `@playwright/test` devDependency + `test:visual` npm script.
- `.gitignore` entries for Playwright runtime output.
</artifacts>

<output>
Create `.planning/phases/01-theme-css-token-centralization/01-01-SUMMARY.md` when done.
</output>

---
phase: 04-bilingual-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - README.md
  - README.en.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "A reader opening README.md sees the shipped feature set (dark/light toggle, PT-BR/EN toggle, six portfolio sections, centralized theme tokens), not the retired TODO list (DOCS-01)"
    - "A reader opening README.en.md sees the same sections and information in English, with content parity to README.md (DOCS-02)"
    - "From either README a reader can click a top-of-file banner link to reach the other language version (DOCS-03)"
    - "Every Getting Started command matches package.json scripts and the Dockerfile verbatim — no invented commands (D-02)"
    - "The License section of both files links to a real root-level MIT LICENSE file (D-05)"
    - "A Live Demo link to https://lsiqueira.dev.br appears near the top of both files (D-06)"
  artifacts:
    - "LICENSE (new, MIT boilerplate, holder Leandro Siqueira, year 2026)"
    - "README.md (rewritten PT-BR, full scope per D-01)"
    - "README.en.md (new EN counterpart, content parity)"
  key_links:
    - "README.md banner link -> README.en.md resolves (relative path)"
    - "README.en.md banner link -> README.md resolves (relative path)"
    - "Both License sections link -> LICENSE resolves (relative path)"
---

<objective>
Rewrite the project README as two cross-linked files — `README.md` (PT-BR, full rewrite) and `README.en.md` (EN, new) — and add a root-level MIT `LICENSE`, so a reader in either language can understand the portfolio's features and tech stack and jump to the other language version.

This is a documentation-only phase (D-01 through D-06): no application code, site content, structure, routes, or existing links change. The single biggest risk is the executor inventing commands or features that do not exist in the repo — every Getting Started command MUST be copied verbatim from `package.json` scripts and the actual `Dockerfile`.

Purpose: Retire the stale 3-item TODO list (all shipped by Phases 1–3) and present the project as a shareable, bilingual, OSS-style repository.
Output: `LICENSE` (new), `README.md` (rewritten), `README.en.md` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-bilingual-readme/04-CONTEXT.md
@.planning/phases/04-bilingual-readme/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `LICENSE` — NEW (MIT license boilerplate)
- `README.md` — REWRITTEN (was a 4-line title + TODO list; becomes the full PT-BR README)
- `README.en.md` — NEW (English counterpart with content parity)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add root-level MIT LICENSE file</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/phases/04-bilingual-readme/04-CONTEXT.md (D-05: new MIT LICENSE at repo root, referenced by both READMEs)
    - .planning/phases/04-bilingual-readme/04-PATTERNS.md (`LICENSE` section — no in-repo analog, use standard MIT boilerplate)
  </read_first>
  <action>
    Create `LICENSE` at the repository root using the standard MIT License text. Copyright line must read year 2026 and copyright holder "Leandro Siqueira" (the project author). Include the standard MIT permission-grant paragraph and the uppercase "AS IS" warranty-disclaimer paragraph exactly as they appear in the canonical MIT template — do not paraphrase or abbreviate the legal text. No file previously exists at this path; this is a new artifact per D-05.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -qi "MIT License" LICENSE && grep -q "Leandro Siqueira" LICENSE && grep -q "2026" LICENSE && grep -qi "WITHOUT WARRANTY" LICENSE</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists at repo root
    - Contains the phrase "MIT License", the copyright holder "Leandro Siqueira", and year 2026
    - Contains the standard MIT warranty-disclaimer language ("WITHOUT WARRANTY")
    - Legal text is the full canonical MIT boilerplate, not an abbreviated summary
  </acceptance_criteria>
  <done>A valid root-level MIT LICENSE file exists that both README License sections can link to.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md (PT-BR) with full-scope content</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 4-line file — the TODO list to retire)
    - package.json (exact scripts: dev, build, start, typecheck, lint, format, test:visual — copy verbatim)
    - Dockerfile (actual multi-stage build: development-dependencies-env, production-dependencies-env, build-env, final stage CMD `npm run start`)
    - .planning/codebase/STACK.md (verified tech versions: React 19.2.6, React Router 7.15.1 SSR, Tailwind CSS 4.2.2, TypeScript 5.9.3, i18next/react-i18next)
    - .planning/codebase/STRUCTURE.md (feature-folder pattern for the Project Structure section)
    - .planning/phases/04-bilingual-readme/04-CONTEXT.md (D-01 full scope, D-02 npm+Docker, D-03 structure overview, D-04 features, D-06 live demo)
    - .planning/phases/04-bilingual-readme/04-PATTERNS.md (verbatim command sources, banner pattern, required section list)
  </read_first>
  <action>
    Fully replace `README.md` with a PT-BR README that has these sections in order: (1) a cross-link banner as the very first line, above the title — Portuguese shown as the active label and a working relative markdown link labeled "English" pointing to `README.en.md`; (2) the project title; (3) a "Live Demo" / "Demonstração" link to `https://lsiqueira.dev.br` placed directly under the banner/title (per D-06); (4) a Features section documenting the shipped milestone feature set per D-04 — the dark/light theme toggle (Phase 2), the PT-BR/EN language toggle (Phase 3), the six portfolio sections (Header, Hero, Projects, AboutMe, Contacts, Footer), and the centralized Tailwind v4 theme-token system (Phase 1) as an engineering highlight; write this as a portfolio pitch of what the site does, not a component-by-component code walkthrough; (5) a Tech Stack section citing the versions from STACK.md / package.json (React, React Router SSR, Tailwind CSS 4, TypeScript, i18next + react-i18next, lucide-react, react-icons, @fontsource/outfit); (6) a Getting Started section documenting BOTH the local npm workflow (`npm install` then `npm run dev`, and note `npm run build` / `npm run start` for production) AND the Docker workflow (`docker build` then `docker run`, noting the container's final CMD is `npm run start`) — every command copied verbatim from package.json and the Dockerfile, inventing no scripts, flags, compose files, or env vars that those source files do not contain; (7) a Project Structure section as a brief overview of the `app/features/{Header,Hero,Projects,AboutMe,Contacts,Footer}` feature-folder pattern where each folder holds `index.tsx` / `data.tsx` / `types.tsx` (per D-03 — a short overview, not a full recursive file tree); (8) a License / "Licença" section stating the project is MIT-licensed with a working relative link to the `LICENSE` file. Remove the entire old TODO list and its heading — the three items it listed are all shipped and are now documented in the Features section instead. Do not alter, translate, or restate any site content, copy, or links; this rewrite only concerns the README document itself.
  </action>
  <verify>
    <automated>test -f README.md && ! grep -q "## TODO" README.md && grep -q "README.en.md" README.md && grep -q "lsiqueira.dev.br" README.md && grep -q "npm run dev" README.md && grep -q "docker build" README.md && grep -q "npm run start" README.md && grep -qi "LICENSE" README.md && [ "$(grep -c '^## ' README.md)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - Old TODO heading/list is gone (no `## TODO` section remains)
    - Banner is the first content and links to `README.en.md`
    - Live Demo link `https://lsiqueira.dev.br` is present near the top
    - Features section names all four D-04 items (theme toggle, language toggle, six sections, centralized theme tokens)
    - Tech Stack versions match STACK.md / package.json (no fabricated versions)
    - Getting Started shows `npm run dev` and the Docker `docker build`/`docker run` flow with container CMD `npm run start`, all matching package.json + Dockerfile verbatim — no invented commands, flags, compose files, or env vars
    - Project Structure is a brief feature-folder overview, not a full recursive tree
    - License section links to `LICENSE`
    - At least 5 top-level (`##`) sections present
  </acceptance_criteria>
  <done>README.md is a complete PT-BR README covering all D-01 sections, with the TODO list retired and all commands verifiably matching source files.</done>
</task>

<task type="auto">
  <name>Task 3: Create README.en.md (EN) with content parity</name>
  <files>README.en.md</files>
  <read_first>
    - README.md (the rewritten PT-BR file from Task 2 — the section-for-section source to mirror)
    - package.json (re-confirm exact scripts for the EN Getting Started)
    - Dockerfile (re-confirm exact Docker steps for the EN Getting Started)
    - .planning/phases/04-bilingual-readme/04-CONTEXT.md (D-02 content parity, "translate meaning, not reword" I18N-06 precedent)
    - .planning/phases/04-bilingual-readme/04-PATTERNS.md (required section list, mirrored banner pattern)
  </read_first>
  <action>
    Create `README.en.md` as the English counterpart of the rewritten `README.md`, with the same sections in the same order and the same information (content parity per DOCS-02). Translate meaning rather than rewording — do not add, drop, or invent sections or facts relative to README.md. The first line is the mirrored cross-link banner: English shown as the active label and a working relative markdown link labeled "Português" pointing to `README.md`. Keep the Live Demo link `https://lsiqueira.dev.br`, and keep all Getting Started commands (npm and Docker) identical and verbatim to package.json and the Dockerfile — commands are code, so they stay in English/literal form and must not be paraphrased. The License section links to the same `LICENSE` file. The number of top-level (`##`) sections must match README.md so the two files stay in parity.
  </action>
  <verify>
    <automated>test -f README.en.md && grep -q "README.md" README.en.md && grep -q "lsiqueira.dev.br" README.en.md && grep -q "npm run dev" README.en.md && grep -q "docker build" README.en.md && grep -q "npm run start" README.en.md && grep -qi "LICENSE" README.en.md && [ "$(grep -c '^## ' README.en.md)" = "$(grep -c '^## ' README.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `README.en.md` exists at repo root
    - Banner is the first content and links back to `README.md`
    - Same section set and order as README.md (content parity), translated in meaning not reworded
    - Live Demo link `https://lsiqueira.dev.br` present
    - Getting Started commands (npm + Docker) are byte-identical to README.md and to package.json / Dockerfile — no divergence, no invented commands
    - License section links to `LICENSE`
    - Top-level (`##`) section count equals README.md's count
  </acceptance_criteria>
  <done>README.en.md is a complete English counterpart with verifiable content parity to README.md and a working back-link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → human/GitHub reader | Static Markdown/LICENSE text is read by humans and rendered by GitHub. No runtime, no user input, no data storage, no auth, no code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | README.md / README.en.md content | low | mitigate | Documentation-only; no secrets, credentials, tokens, or internal URLs are included — only the public live-demo URL and public tech-stack facts. |
| T-04-02 | Tampering | Getting Started commands | low | mitigate | Risk is inaccurate/invented commands misleading a reader, not a security exploit. Mitigated by copying every command verbatim from package.json and Dockerfile and verifying via grep gates. |
| T-04-SC | Tampering | package-manager installs | n/a | accept | No npm/pip/cargo install tasks in this phase — nothing is installed or added to dependencies. Supply-chain gate is not applicable; no Package Legitimacy Audit required. |

ASVS level: 1. Block-on severity: high. No high/critical threats — this phase produces only static Markdown and a LICENSE file with no attack surface (no user input, auth, data storage, or runtime code).
</threat_model>

<verification>
- `LICENSE` exists, is valid MIT boilerplate (holder Leandro Siqueira, 2026, warranty disclaimer)
- `README.md` retired the old TODO section and covers all D-01 sections (banner, title, Live Demo, Features, Tech Stack, Getting Started npm+Docker, Project Structure, License)
- `README.en.md` exists with matching section count and content parity
- All three cross-links resolve as relative paths: README.md ↔ README.en.md, and both → LICENSE
- Every Getting Started command matches package.json scripts and the Dockerfile verbatim (no invented commands/flags/compose files/env vars)
- Combined gate: `test -f LICENSE && test -f README.en.md && ! grep -q "## TODO" README.md && grep -q "README.en.md" README.md && grep -q "README.md" README.en.md && [ "$(grep -c '^## ' README.md)" = "$(grep -c '^## ' README.en.md)" ]`
</verification>

<success_criteria>
- README.md (PT-BR) rewritten highlighting features and tech stack — DOCS-01 satisfied
- README.en.md (EN) created with content parity to README.md — DOCS-02 satisfied
- Both files carry a top-of-file cross-link banner so a reader can jump between language versions — DOCS-03 satisfied
- Root-level MIT LICENSE exists and is referenced from both files — D-05 satisfied
- No site code, content, structure, routes, or existing links changed
</success_criteria>

<output>
Create `.planning/phases/04-bilingual-readme/04-01-SUMMARY.md` when done.
</output>
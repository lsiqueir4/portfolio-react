---
phase: 03-language-toggle-i18n
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/features/Hero/index.tsx
  - app/features/Contacts/index.tsx
  - app/features/Footer/index.tsx
  - app/i18n/locales/pt.ts
  - app/i18n/locales/en.ts
autonomous: true
requirements: [I18N-06, I18N-08]

must_haves:
  truths:
    - "Switching PT/EN translates every user-facing string in Hero, Contacts, and Footer 1:1"
    - "Content, links, and images in these three sections are byte-identical between languages — only text changes (I18N-08)"
  artifacts:
    - app/i18n/locales/pt.ts
    - app/i18n/locales/en.ts
    - app/features/Hero/index.tsx
    - app/features/Contacts/index.tsx
    - app/features/Footer/index.tsx
  key_links:
    - "Hero/Contacts/Footer resolve strings through useTranslation()/t() against hero/contacts/footer sections in the locale catalogs"
    - "CONTACTS values (email, phone, URLs, name) and image imports remain untouched"
---

<objective>
Translate the three JSX-only sections — Hero, Contacts, Footer — by wiring `useTranslation()`/`t()` into their render paths and extending the `pt`/`en` locale catalogs created in Plan 01. These sections carry NO array-literal `data.tsx` content, so they are a clean second slice on top of the working toggle.

Purpose: Extend the phase's working language switch to the hero, contact, and footer copy without touching contact data, links, or images (I18N-08).
Output: `hero`, `contacts`, and `footer` sections added to `app/i18n/locales/{pt,en}.ts`; the three feature `index.tsx` files rendering through `t()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-language-toggle-i18n/03-CONTEXT.md
@.planning/phases/03-language-toggle-i18n/03-01-SUMMARY.md
@app/features/Hero/index.tsx
@app/features/Contacts/index.tsx
@app/features/Footer/index.tsx
@app/i18n/config.ts
@app/i18n/locales/pt.ts
</context>

## Phase Goal

**As a** visitor of the portfolio, **I want to** read the hero, contact, and footer copy in my chosen language, **so that** the whole landing experience switches with the toggle, not just the nav.

## Artifacts this phase produces (Plan 02)

Extends existing artifacts only — adds `hero`, `contacts`, `footer` sections to the Plan 01 locale catalogs. No new files.

<tasks>

<task type="auto">
  <name>Task 1: Translate the Hero section</name>
  <files>app/features/Hero/index.tsx, app/i18n/locales/pt.ts, app/i18n/locales/en.ts</files>
  <read_first>
    - app/features/Hero/index.tsx (all hardcoded PT strings; note CONTACTS.fullName / cvDownloadLink / techs stay as-is)
    - app/i18n/locales/pt.ts (existing header/common sections + Plan 01 structure to extend)
    - app/i18n/locales/en.ts
    - .planning/phases/03-language-toggle-i18n/03-01-SUMMARY.md (catalog structure decided in Plan 01)
  </read_first>
  <action>
    Add a `hero` section to app/i18n/locales/pt.ts and en.ts with keys for every user-facing Hero string: the availability badge, the greeting line, the role heading, and the intro paragraph. PT values are the exact current strings ("Disponível para oportunidades", "Olá, eu sou", "Desenvolvedor Full-Stack", and the full intro paragraph about REST APIs / banking integrations / credit products); EN values are faithful 1:1 translations that preserve meaning and add nothing new. Reuse `common.downloadCV` for the "Baixar CV" button and add/reuse a common key for the "Entre em contato" secondary button. In app/features/Hero/index.tsx, add `const { t } = useTranslation();` and replace each hardcoded string with the matching t() call. Do NOT translate the `techs` names (proper nouns), and do NOT change CONTACTS.fullName, the profile image, hrefs, or layout (I18N-08).
  </action>
  <verify>
    <automated>npm run typecheck && grep -q "useTranslation" app/features/Hero/index.tsx && grep -q "hero" app/i18n/locales/pt.ts && grep -q "hero" app/i18n/locales/en.ts</automated>
  </verify>
  <acceptance_criteria>
    - Hero renders availability badge, greeting, role, and intro paragraph via t()
    - pt.ts and en.ts both gained a structurally identical `hero` section
    - CONTACTS.fullName, profile image, tech badges, and all hrefs are unchanged
  </acceptance_criteria>
  <done>Hero copy switches language on toggle; content/links/images unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Translate the Contacts and Footer sections</name>
  <files>app/features/Contacts/index.tsx, app/features/Footer/index.tsx, app/i18n/locales/pt.ts, app/i18n/locales/en.ts</files>
  <read_first>
    - app/features/Contacts/index.tsx (heading, description paragraph, and the WhatsApp/Email/Linkedin labels; CONTACTS values are links/data — keep)
    - app/features/Footer/index.tsx (the single copyright string with dynamic year + CONTACTS.fullName)
    - app/i18n/locales/pt.ts / en.ts (extend the same catalogs)
  </read_first>
  <action>
    Add a `contacts` section and a `footer` section to app/i18n/locales/pt.ts and en.ts.
    Contacts keys: the section heading ("Entre em Contato"), the availability/description paragraph, and the three channel labels ("WhatsApp", "Email", "Linkedin"). WhatsApp is a brand name — keep it identical in both languages; "Email"/"Linkedin" likewise stay as-is but route through keys for consistency. EN values are faithful translations of the heading and paragraph. In app/features/Contacts/index.tsx add useTranslation and replace the heading, paragraph, and labels with t() calls; leave CONTACTS.formattedPhone, CONTACTS.email, CONTACTS.fullName, all hrefs, icons, and the mailto/wa.me/linkedin links untouched (I18N-08).
    Footer key: the copyright sentence. Use interpolation so the dynamic year and CONTACTS.fullName are injected (e.g. a key like `footer.rights` with a `{{year}}` placeholder, or split so `new Date().getFullYear()` and CONTACTS.fullName remain runtime values); the translatable part is only "Todos os direitos reservados." → "All rights reserved." In app/features/Footer/index.tsx add useTranslation and render the copyright via t(), keeping the year computation and CONTACTS.fullName as runtime values.
  </action>
  <verify>
    <automated>npm run typecheck && grep -q "useTranslation" app/features/Contacts/index.tsx && grep -q "useTranslation" app/features/Footer/index.tsx && grep -q "contacts" app/i18n/locales/en.ts && grep -q "footer" app/i18n/locales/en.ts</automated>
  </verify>
  <acceptance_criteria>
    - Contacts heading, description, and channel labels render via t(); phone/email/URLs unchanged
    - Footer copyright renders via t() with the year and full name kept as runtime values
    - pt.ts and en.ts both gained structurally identical `contacts` and `footer` sections
  </acceptance_criteria>
  <done>Contacts and Footer copy switch language on toggle; all contact data, links, and the dynamic year are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translation catalog → JSX render | static author-controlled strings rendered via t() |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Injection (XSS) | hero/contacts/footer strings rendered via t() | low | accept | Strings are static and author-controlled; rendered as plain t() text (React auto-escapes). Do NOT route any translation through dangerouslySetInnerHTML. Footer interpolation uses i18next `{{year}}` placeholders with escaping on, not string concatenation into markup. |
| T-03-04 | Tampering | CONTACTS links/email/phone (must remain unchanged) | low | accept | Task actions explicitly forbid touching CONTACTS values, hrefs, and images; only visible text strings are keyed (I18N-08 preserved). |

No new install surface in this plan; no high-severity threat introduced.
</threat_model>

<verification>
- `npm run typecheck` passes.
- `npx playwright test tests/language-toggle.spec.ts` still passes (Header slice unregressed).
- Manual: toggling EN translates the hero badge/greeting/role/intro, the contacts heading/paragraph/labels, and the footer copyright; the profile image, phone, email, LinkedIn/WhatsApp links, and copyright year are identical in both languages.
</verification>

<success_criteria>
- I18N-06 (Hero/Contacts/Footer slice): every user-facing string in these three sections is translated 1:1 into English.
- I18N-08: content, links, and images in these sections are unchanged — only the language of existing text changes.
</success_criteria>

<output>
Create `.planning/phases/03-language-toggle-i18n/03-02-SUMMARY.md` when done, noting the final key names added under hero/contacts/footer so Plan 03 avoids collisions.
</output>

---
phase: 02-dark-mode-toggle
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/theme-light.spec.ts
autonomous: false
requirements: [DARK-02, DARK-04]
user_setup: []

must_haves:
  truths:
    - "A light-mode visual baseline exists so future changes to the light theme are caught by regression"
    - "A human has confirmed the toggle works in desktop + mobile, no flash on reload, purple accent identical in both themes, and content/links/images unchanged"
  artifacts:
    - tests/theme-light.spec.ts
  key_links:
    - "theme-light.spec.ts forces the light theme (no stored choice) and screenshots the same sections the dark baseline covers, giving a per-theme regression net"
---

<objective>
Harden and sign off the theme toggle shipped in Plan 01. Add a light-mode visual-regression baseline (the dark baseline is already pinned in `tests/visual.spec.ts`), then run a human verification checkpoint covering the DARK-02 no-flash experience and the DARK-04 accent-parity/content-preservation guarantees that automated screenshots alone cannot fully certify.

Purpose: Lock in a per-theme regression net and get explicit human confirmation before the phase is considered done.
Output: `tests/theme-light.spec.ts` light baselines + a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dark-mode-toggle/02-RESEARCH.md
@tests/visual.spec.ts
@playwright.config.ts
@.planning/phases/02-dark-mode-toggle/02-01-SUMMARY.md
</context>

## Artifacts this phase produces

- **New file:** `tests/theme-light.spec.ts` — Playwright visual spec capturing the light theme (default, no stored choice) for the same sections the dark baseline covers.

<tasks>

<task type="auto">
  <name>Task 1: Add light-mode visual-regression baselines</name>
  <files>tests/theme-light.spec.ts</files>
  <read_first>
    - tests/visual.spec.ts (section locators and screenshot conventions to mirror: header, #inicio, #projetos, #aboutme, #contacts, footer, full page)
    - playwright.config.ts (toHaveScreenshot tolerance + chromium project)
  </read_first>
  <action>
    Create tests/theme-light.spec.ts mirroring the structure of tests/visual.spec.ts, but capturing the LIGHT theme. In beforeEach, force light via page.addInitScript that sets localStorage "theme" to "light" (so the result is deterministic regardless of the runner's OS/emulated color scheme), then goto "/", waitForLoadState "networkidle", and await document.fonts.ready. Use distinct screenshot names (e.g. suffix `-light`) so they do not collide with the dark baselines. Cover the same targets as the dark spec: full page, header, hero (#inicio), projects (#projetos), about me (#aboutme), contacts (#contacts), footer. On first run Playwright writes the new light baselines; commit them as the light regression reference.
  </action>
  <verify>
    <automated>npx playwright test tests/theme-light.spec.ts --update-snapshots && npx playwright test tests/theme-light.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/theme-light.spec.ts exists, forces the light theme deterministically, and produces `-light` screenshot baselines for all seven targets.
    - A second run passes cleanly against the freshly written light baselines.
  </acceptance_criteria>
  <done>Light-mode visual baseline captured and passing; both themes now have a regression net.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — toggle, no-flash, accent parity, content preservation</name>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps below, then wait for the resume signal before marking the phase complete.</action>
  <what-built>
    A Header sun/moon toggle (desktop nav + always-visible mobile control) that switches between light and dark themes, persists the choice in localStorage, defaults to the browser's `prefers-color-scheme` on first visit, and applies the theme before paint via a blocking script in the document head. Automated specs already confirm toggle behavior, persistence, pre-paint apply, and that the dark theme matches the Phase 1 baseline byte-for-byte.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173.
    2. Desktop: confirm a sun/moon button sits in the top nav next to the "Baixar CV" button. Click it — the whole site should switch light<->dark. The purple accent (logo `</>`, primary CV button fill, nav hover underline) must look like the SAME purple in both themes; only backgrounds/text should invert.
    3. Keyboard: Tab to the toggle, press Enter/Space — it should toggle. Screen-reader/inspect: it is a native `<button>` with an aria-label and aria-pressed reflecting state.
    4. Persistence + no flash: switch to light, reload the page — it should come back light with NO dark flash before paint. Repeat for dark. Then in devtools set `localStorage.theme` to "light" while your OS is in dark mode and reload — the site must stay light (manual override beats OS).
    5. Mobile (narrow the window below md / device toolbar): the toggle must be visible WITHOUT opening the hamburger menu, and still work.
    6. Content check: confirm no text, link, image, or section layout changed in either theme — only colors.
  </how-to-verify>
  <resume-signal>Type "approved", or describe any flash, misaligned accent, unreachable toggle, or content/layout change to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds only a test file and a human review; it introduces no new runtime trust boundary. The runtime boundaries (`localStorage` -> client JS, and the `dangerouslySetInnerHTML` blocking script) are owned and mitigated by Plan 01's threat model (T-02-01, T-02-02).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | Playwright baseline images committed to the repo | low | accept | Visual baselines are non-executable reference PNGs; a tampered baseline only weakens regression sensitivity, caught by the human sign-off in this plan. No mitigation beyond normal code review |
</threat_model>

<verification>
- `npx playwright test tests/theme-light.spec.ts` passes against the newly written light baselines.
- Human checkpoint approved (toggle, no-flash, accent parity, content preservation).
</verification>

<success_criteria>
- Light-mode visual baseline exists and passes.
- Human has confirmed DARK-01..04 behavior in a running app with no content/layout regression.
</success_criteria>

<output>
Create `.planning/phases/02-dark-mode-toggle/02-02-SUMMARY.md` when done.
</output>
